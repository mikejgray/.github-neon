name: Docker Build Tests
on:
  workflow_call:
    inputs:
      runner:
        type: string
        default: "ubuntu-latest"
jobs:
  docker_build_tests:
    timeout-minutes: 15
    runs-on: ${{inputs.runner}}
    steps:
      - uses: actions/checkout@v2
      - name: Build image
        run: docker build .
